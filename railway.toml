[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m http.server 8080 --directory ."
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
type = "web"
